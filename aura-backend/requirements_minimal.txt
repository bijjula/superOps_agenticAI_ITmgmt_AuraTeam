# Minimal requirements for Python 3.13 compatibility
# These packages have pre-built wheels and don't require compilation

# Core FastAPI and server
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6

# Basic database support (without async PostgreSQL for now)
pymongo>=4.0.0
motor>=3.0.0
redis>=4.0.0

# Data validation and serialization  
pydantic>=2.0.0
pydantic-settings>=2.0.0

# OpenAI for AI features
openai>=1.0.0

# HTTP client for external APIs
httpx>=0.24.0
requests>=2.28.0

# Environment and configuration
python-dotenv>=1.0.0

# Authentication and security
python-jose>=3.3.0
passlib>=1.7.0

# Logging and monitoring
structlog>=20.0.0

# Date and time utilities
python-dateutil>=2.8.0

# Basic ORM without async (fallback)
sqlalchemy>=2.0.0

# Development and testing
pytest>=7.0.0
